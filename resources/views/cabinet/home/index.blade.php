@extends('layouts.sidebar')

@section('content')
    <div class="row justify-content-center">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">{{ __('Cabinet') }}</div>

                <div class="card-body">
                    {{ __('Cabinet!') }}
                </div>
            </div>
        </div>
    </div>
@endsection